import test from 'node:test';import assert from 'node:assert/strict';import {evaluateInterventionOutcome,outcomeMetricLabel} from './functions/_shared/cgma-intervention-outcome.js';
test('keeps collecting before review day',()=>{const r=evaluateInterventionOutcome({baseline:{directions:4},current:{directions_click:6},metric:'directions_click',today:'2026-09-14',review_day:'2026-09-15'});assert.equal(r.state,'collecting');assert.equal(r.recommendation,null)});
test('marks a well-sampled lift as effective',()=>{const r=evaluateInterventionOutcome({baseline:{directions:8},current:{directions_click:12},metric:'directions_click',today:'2026-09-15',review_day:'2026-09-15'});assert.equal(r.state,'effective');assert.equal(r.recommendation,'keep');assert.equal(Math.round(r.change*100),50)});
test('does not overstate tiny samples',()=>{const r=evaluateInterventionOutcome({baseline:{total:1},current:{total:2},metric:'all',today:'2026-09-15',review_day:'2026-09-15'});assert.equal(r.state,'inconclusive');assert.equal(r.recommendation,'iterate')});
test('marks a well-sampled decline for redesign',()=>{const r=evaluateInterventionOutcome({baseline:{total:10},current:{total:5},metric:'all',today:'2026-09-15',review_day:'2026-09-15'});assert.equal(r.state,'redesign');assert.equal(r.recommendation,'iterate')});
test('recognizes new signal from a zero baseline',()=>{const r=evaluateInterventionOutcome({baseline:{directions:0},current:{directions_click:5},metric:'directions_click',today:'2026-09-15',review_day:'2026-09-15'});assert.equal(r.state,'effective');assert.equal(outcomeMetricLabel('directions_click'),'길찾기')});
